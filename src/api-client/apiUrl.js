const baseApiUrl = 'http://localhost:3001';

// List of API.
const apiUrl = {
    // User.
    register: `${baseApiUrl}/user/register`,
    //login: `${baseApiUrl}/user/login`,
    //profile: `${baseApiUrl}/user/profile`,

}

export default apiUrl